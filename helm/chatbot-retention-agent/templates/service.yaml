apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatbot-retention-agent.fullname" . }}
  labels:
    {{- include "chatbot-retention-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chatbot-retention-agent.selectorLabels" . | nindent 4 }}
